# 每 30 分钟触发一次体能统计迟到补采到期判断(allwin-physical-stats.service)。
[Unit]
Description=Run allwin physical stats re-poll every 30 minutes

[Timer]
OnBootSec=5min
OnUnitActiveSec=30min
Persistent=true

[Install]
WantedBy=timers.target
